{
  "name": "benchpress-rs",
  "version": "0.1.0",
  "description": "BenchpressJS compiler written in Rust for speed",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/benchpressjs/benchpress-rs"
  },
  "author": "Peter Jaszkowiak <p.jaszkow@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "neon-cli": "^0.3.3"
  },
  "scripts": {
    "test": "cd benchpress_sys && cargo test && cd .. && neon build --release && node -p 'require(\"./\").compile(\"{{thing}}\")'",
    "install": "neon build --release >> build.log 2>&1 || exit 0",
    "release": "node scripts/release",
    "build-configs": "node scripts/build-configs"
  }
}
